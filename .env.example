VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Only needed to run `npm run seed:books` (scripts/seed-books.js). This key
# bypasses RLS — get it from Dashboard -> Settings -> API and never commit it.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
